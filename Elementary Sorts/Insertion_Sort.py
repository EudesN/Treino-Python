def insertion_sort(lista):
    n = len(lista)
    for i in range(1, n - 1): # O primeiro elemento é ignirádo pois uma lista com um unico elemento já está ordenada
        chave = lista[i]
        j = i - 1
        while j >=0 and lista[j]








